machine:
  python:
    version: [2.6.8, 2.7, pypy-2.2.1, 3.3.3, 3.4.1]

dependencies:
  pre:
    - python -c 'import setup; print(setup.REQUIRES_PIP)' |xargs pip install
    - pip install .

test:
  post: coveralls_multi_ci submit -v
