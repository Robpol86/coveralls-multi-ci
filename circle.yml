machine:
  python:
    version: 3.4.1

dependencies:
  override:
    - python -c 'import setup; print(setup.REQUIRES_PIP)' |xargs pip install
    - pip install .

test:
  post:
    - coveralls_multi_ci submit -v
    - coveralls_multi_ci submit -v --multi
